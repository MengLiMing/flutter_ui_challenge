import 'package:flutter/material.dart';

class DeerHomePage extends StatelessWidget {
  const DeerHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Home'),
      ),
    );
  }
}
